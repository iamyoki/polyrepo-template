import { a } from "../lib/a.ts";
import { b } from "../lib/b.ts";
import { c } from "../lib/c.ts";

// I'm a running app, could be a node server.
console.log(a, b, c);
console.log(`I'm a running app, could be a node server....`);
